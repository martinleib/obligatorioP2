@model List<LogicaNegocio.Cliente>

@{
    ViewData["Title"] = "Index";
    string tipoDeUsuario = Context.Session.GetString("logged-user-type");
}

@if (tipoDeUsuario == "Cliente")
{
    <h1>Mi perfil</h1>
    <table class="table">
        <thead>
        <tr>
            <th>Nombre</th>
            <th>Apellido</th>
            <th>Email</th>
            <th>Saldo</th>
            <th>Acción</th>
        </tr>
        </thead>
        <tbody>
        @foreach (LogicaNegocio.Cliente cliente in Model) {
            <tr>
                <td>@cliente.Nombre</td>
                <td>@cliente.Apellido</td>
                <td>@cliente.Email</td>
                <td>@cliente.Saldo</td>
                <td>
                    <a asp-controller="Cliente" asp-action="Edit">Cargar saldo</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
}
else if (tipoDeUsuario == "Administrador")
{
    <h1>Listado de clientes</h1>
    @if (Model != null && Model.Count > 0)
    {
        <table class="table">
            <thead>
            <tr>
                <th>Nombre</th>
                <th>Apellido</th>
                <th>Email</th>
                <th>Saldo</th>
            </tr>
            </thead>
            <tbody>
            @foreach (LogicaNegocio.Cliente cliente in Model)
            {
                <tr>
                    <td>@cliente.Nombre</td>
                    <td>@cliente.Apellido</td>
                    <td>@cliente.Email</td>
                    <td>@cliente.Saldo</td>
                </tr>
            }
            </tbody>
        </table>
    }
    else
    {
        <p>No existen clientes en el sistema</p>
    }
}
else
{
    <p>No se ha identificado el tipo de usuario.</p>
}

<p>@TempData["Mensaje"]</p>
